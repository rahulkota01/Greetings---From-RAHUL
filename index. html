<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Special Greeting</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(to right, #00c6ff, #0072ff);
      color: white;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    input, button {
      padding: 10px;
      margin: 10px;
      border: none;
      border-radius: 8px;
    }
    button {
      background-color: #ffffff;
      color: #0072ff;
      cursor: pointer;
      font-weight: bold;
    }
    .greeting {
      margin-top: 20px;
      font-size: 1.5em;
      font-weight: bold;
      animation: bounceIn 0.8s ease;
      transform-origin: center;
    }

    @keyframes bounceIn {
      0% {
        opacity: 0;
        transform: scale(0.3);
      }
      50% {
        opacity: 1;
        transform: scale(1.05);
      }
      70% {
        transform: scale(0.9);
      }
      100% {
        transform: scale(1);
      }
    }
  </style>
</head>
<body>
  <h1>Welcome!</h1>
  <p>Enter your name to receive something special:</p>
  <input type
